//header guard - prevents the same header from being included multiple times.
#ifndef PRACTICEHEADER_H    //if the header isn't already there ...
#define PRACTICEHEADER_H    //define it

void doPrint(std::string &toPrint);

#endif //terminate the ifndef block